# flask-tutorial
## activate venv.
- `$ mkdir myproject`
- `$ cd myproject`
- `$ python3 -m venv .venv`

## Initialize DB
`flask --app flaskr init-db`

## Install project in an env
`pip install -e .`